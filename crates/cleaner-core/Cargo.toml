[package]
name = "cleaner-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Portable logic for osu-lazer-cleaner: storage discovery, reference extraction, scanning, planning, and snapshots"

[lints]
workspace = true

[dependencies]
cleaner-realm = { version = "=1.2.0", path = "../cleaner-realm" }
jiff.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
tempfile.workspace = true
thiserror.workspace = true
tracing.workspace = true
walkdir.workspace = true

[dev-dependencies]
cleaner-realm = { version = "=1.2.0", path = "../cleaner-realm", features = ["test-support"] }
tempfile.workspace = true

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true }
