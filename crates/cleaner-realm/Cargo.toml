[package]
name = "cleaner-realm"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Safe Rust bindings to realm-core's C API, scoped to what an osu!lazer library cleaner needs"

[features]
# Exposes `fixture`, which builds a scratch database. Tests only, never a release build.
test-support = []

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
tracing.workspace = true

[build-dependencies]
bindgen.workspace = true
cmake.workspace = true

[dev-dependencies]
serde_json.workspace = true
tempfile.workspace = true
